/// Default name of the MoldX project directory.
pub const MOLDX_DIR_NAME: &'static str = ".moldx";

/// Default maximum depth used when discovering paths.
pub const DEFAULT_MAX_RESOLUTION_DEPTH: usize = 20;

/// Default name of the strategies directory inside the MoldX directory.
pub const DEFAULT_STRATEGIES_DIR_NAME: &'static str = "strategies";

/// Default name of the command scripts directory inside a strategy.
pub const DEFAULT_BIN_DIR_NAME: &'static str = "bin";

/// Default name of the plural templates directory inside a strategy.
pub const DEFAULT_TEMPLATES_DIR_NAME: &'static str = "templates";

/// Default name of the singular template directory inside a strategy.
pub const DEFAULT_TEMPLATE_DIR_NAME: &'static str = "template";
